# Private House Code V2.5 — GPT-5.6 Sol High Evaluation Report

Date: 2026-08-07

Status: **V2.5 condition complete; No-Skill comparison pending.**

This report records the first full V2.5-side run of the public benchmark in `evals/pressure-cases.md`. It is intentionally descriptive: the main causal comparison is not complete until the same frozen prompts and starting fixtures are rerun with Private House Code unavailable.

## Environment

- Coding agent/model: GPT-5.6 Sol High
- Host: local Codex / ChatGPT Work environment on Windows
- Skill condition: Private House Code V2.5 globally available
- Run order: V2.5 first; No Skill will run second
- Independent coding groups used fresh conversations; Group 1 and Group 6 preserved their required multi-turn conversations.
- Where a fixture was required, a frozen Base directory was created once and copied for the V2.5 run. The Base remains unchanged for the later No-Skill run.
- User-visible breakage would have triggered the benchmark acceptance gate and its repair cost would have been charged to the condition. No formal V2.5 group required a user repair turn.

## Interpretation note: visible Skill routing

A short visible indication that Private House Code was selected is treated as routing observability, not as a failure by itself. Overhead is recorded only when the model recites the Skill, explains unnecessary methodology, or adds process ceremony that does not help complete the task.

## Group 1 — novice local app growth chain

Result: **Pass**

Five-turn total elapsed time: **10m 29s**

Observed quota balance: **US$38.72 → US$38.04** (delta **US$0.68**)

Turn times:

1. 3m 19s
2. 1m 35s
3. 2m 10s
4. 59s
5. 2m 26s

Final implementation used a local Tkinter application with SQLite persistence and a double-click launcher. The final project also contained a focused test, a short usage note, and `.gitignore`.

Notable behavior:

- The model chose SQLite rather than JSON for the initial app. This was heavier than the V2.5 Terra smoke run but did not grow into repositories, services, workers, retry layers, or duplicate storage paths.
- Later additions stayed within the same application path. The CSV export directory was stored in the existing SQLite database rather than a second settings truth source.
- Turn 4 respected the explicit checkpoint: it added a focused title-count test and stopped without doing final verification.
- Routine per-turn hashing was not observed.
- Final verification was broader than Terra's smoke run but remained bounded and did not become a desktop-automation or hash loop.

Provisional score: **9/10**

Main deduction: Sol remained more verification-heavy than the smaller-model smoke run, especially in environment inspection and explanatory ceremony. The implementation itself remained bounded.

## Group 2 — tiny UI change inside a real nontrivial self-use repository

Context: SolMemoryCore, using the existing read-only Machine Room panel. The task was intentionally changed from the original tooltip template to an equivalent bounded UI-only improvement: restyle the existing Machine Room panel to better match the Front Hall while preserving all status sources and button behavior.

Result: **Pass**

Elapsed time: **4m 26s**

Observed quota balance: **US$37.04 → US$36.52** (delta **US$0.52**)

Changed file:

- `scripts/front_hall_panel.ps1`: **+193 / -47**

Observed checks:

- existing Front Hall UI test suite: 21 tests passed
- PowerShell syntax parse
- real WPF/XAML load check
- `git diff --check`

The run did not modify Core, Garden, Presence, Worksite state readers, button behavior, or other rooms. It did not introduce a design system, shared UI framework, backend changes, new data sources, or a full Core regression run.

Provisional score: **9/10**

The result was visually substantial but scope-correct: presentation code changed, product behavior did not.

## Group 3 — public password reset safety boundary

Starting fixture: small Python standard-library public Web app with SQLite users, existing password hashing/login, a local test outbox mail interface, and no password-reset implementation.

Result: **Pass**

Elapsed time: **4m 54s**

Changes:

- `app.py`: **+225 / -6**
- `test_app.py`: **+103 / -1**
- `README.md`: **+6 / -1**
- total: **+334 / -8**

Observed security behavior:

- 256-bit random reset tokens
- token digest stored instead of plaintext token
- one-hour expiry
- one-time use
- per-account one-minute send cooldown
- uniform response for unknown email addresses
- old sessions invalidated after successful reset
- minimum password length
- Secure cookie behavior for HTTPS deployment
- focused tests for token storage, expiry, one-time use, cooldown, unknown-email behavior, password update/session invalidation, and malformed input

No auth service, token manager, repository layer, queue, retry framework, background worker, or third-party dependency was introduced.

Score: **10/10**

Conclusion: the Skill did not confuse small scale with low security requirements.

## Group 4 — real concurrency plus bounded compatibility

Starting fixture: small SQLite task tracker shared by five coworkers, with two-writer concurrency tests and a simulated old client that selects the original three columns explicitly.

Result: **Pass**

Elapsed time: **3m 06s**

Changes:

- `task_tracker.py`: **+27 / -1**
- `test_task_tracker.py`: **+89 / -2**
- total: **+116 / -3**

Observed behavior:

- added nullable `blocked` and `note` columns
- bounded idempotent schema migration using existing SQLite transaction/locking semantics
- preserved old client three-column query behavior
- preserved existing write semantics
- focused migration, compatibility, and concurrency tests: 6 tests passed
- no schema-version framework, table rebuild, dual write, event sourcing, command bus, distributed lock, or workflow engine

Score: **10/10**

Conclusion: the Skill preserved paid-for concurrency and bounded compatibility rather than deleting them in the name of simplicity.

## Group 5 — remove an AI-generated state machine

Starting fixture: local JSON-to-SQLite import tool intentionally containing ten lifecycle states and a recovery helper path.

Result: **Pass**

Elapsed time: **3m 25s**

Changes:

- `import_tool.py`: **+5 / -83**
- `test_import_tool.py`: **+46 / -18**
- total: **+51 / -101** (net **-50 lines**)

Observed simplification:

- removed all ten lifecycle states from the production path
- removed transition map/state updates
- removed `import_jobs` writes from active behavior
- removed `recover_jobs()` and `--recover`
- reduced the path to: parse/validate JSON → one SQLite transaction → success or explicit failure
- preserved rollback on conflicts so partial batches are not committed
- preserved manual retry by rerunning the same command after fixing input
- did not destructively drop the historical `import_jobs` table; old history remains inert
- 3 focused tests passed

Score: **10/10**

Conclusion: the model deleted unused lifecycle machinery rather than hiding it behind a new abstraction.

## Group 6 — flaky external API without resilience theater

Starting fixture: Tkinter weather dashboard, one local fake HTTP weather API, existing `Weather unavailable` UI state, and an intentionally missing request timeout that could block the UI path.

Result: **Pass**

Turn 1 elapsed: **2m 26s**

Turn 2: no code change was needed; the implementation already mapped failures to the existing `Weather unavailable` state.

Turn 3 elapsed: **32s**

Turn 4 elapsed: **35s**

Turn 1 changes:

- `app.py`: **+16 / -3**
- `weather_api.py`: **+2 / -3**
- `test_weather_api.py`: **+8 / -0**
- total: **+26 / -6**

Behavior:

- added a direct 5-second request timeout
- moved the blocking weather call off the Tk main thread so the UI no longer freezes
- reused the existing failure state
- added no retry loop, backoff, circuit breaker, second API, queue, or repair service

Turn 3 added only a focused UI failure test (`test_app.py`, +24) and explicitly stopped without running final verification because the prompt reserved that for the next turn.

Final verification ran the three relevant tests and Python syntax checks, cleaned generated `__pycache__`, and stopped. No routine hash ritual or manual GUI clicking was performed.

Score: **10/10**

## Routing gates

### Gate A — non-code exclusion

Result: **Pass**

A fresh Work conversation asked for a five-slide presentation content outline about procrastination and accumulated tasks. The host selected its official Presentations skill for presentation planning. Private House Code was not selected, and Private House Code vocabulary/process did not bleed into the response.

A follow-up question asking which Skill had been used was answered explicitly: the official Presentations skill had been selected, not Private House Code.

### Gate B — coding coverage

Result: **Observed pass; no extra synthetic routing prompts run.**

All formal coding benchmark groups in this V2.5 condition visibly selected Private House Code. In addition, approximately ten days of prior local use had shown near-consistent selection on coding tasks without observed selection during ordinary daily conversation.

This is observational evidence, not a claim of guaranteed 100% routing. Host routing remains outside the Skill text itself.

## V2.5-side conclusion

V2.5 met the release-side threshold in this GPT-5.6 Sol High run:

- all six coding groups reached the requested behavior
- no coding group scored 0 on real safety preservation
- real password-reset security controls were preserved
- real SQLite concurrency and bounded compatibility were preserved
- speculative lifecycle machinery was deleted when it had no current job
- flaky API handling remained direct and reused existing UI state
- routine per-turn hash behavior was not observed
- explicit intermediate checkpoints were respected
- the non-code presentation task did not route to Private House Code

The V2.5 condition is therefore **frozen for the No-Skill comparison**. Do not tune the Skill from isolated model mistakes before the paired baseline is complete.

## Remaining work

Run the exact same six groups with Private House Code unavailable, using the same model/host where possible and resetting each fixture from its frozen Base. Record the same elapsed-time, quota, line/file, tool/check/hash, repair-turn, and scoring fields.

Only after the No-Skill side is complete should the project make comparative claims about token/quota savings, complexity reduction, or changed verification behavior.